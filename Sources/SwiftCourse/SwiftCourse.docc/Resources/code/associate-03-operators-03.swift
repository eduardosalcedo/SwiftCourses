// 3.2 - Logical operators
let isRaining = true
let hasUmbrella = false

print(isRaining && hasUmbrella) // AND → false
print(isRaining || hasUmbrella) // OR  → true
print(!isRaining)               // NOT → false
